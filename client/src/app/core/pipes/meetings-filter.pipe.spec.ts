import { MeetingsFilterPipe } from './meetings-filter.pipe';

describe('MeetingsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MeetingsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
